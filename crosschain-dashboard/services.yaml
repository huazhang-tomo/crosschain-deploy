apiVersion: v1
kind: Service
metadata:
  name: crosschain-dashboard
spec:
  ports:
    - port: 5010
      protocol: TCP
      targetPort: http
  selector:
    environment: prod
    app.kubernetes.io/name: crosschain-dashboard
    app.kubernetes.io/instance: crosschain-dashboard

